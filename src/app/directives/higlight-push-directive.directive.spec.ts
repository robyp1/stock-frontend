import { HiglightPushDirectiveDirective } from './higlight-push-directive.directive';

describe('HiglightPushDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HiglightPushDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
